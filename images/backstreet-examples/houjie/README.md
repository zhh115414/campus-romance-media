# 后街差分：巷灯（三张递进）

后街 APP 记忆条目「巷灯」的配图。三张同一场景、同一机位，只差衣物状态，按亲密程度递进，编号即剧情先后。仅用于后街 APP，不参与主卡立绘绑定。

- 大图 1 `01.png`：832 × 1216，1,765,523 字节；SHA-256 `05c61594b2d2a3eafb4371a5ac6a97e792dd546a9f394d52f5ce678473c4bad1`。衣物完整。
- 大图 2 `02.png`：832 × 1216，1,742,403 字节；SHA-256 `a222f220d2baaa5cac3d411766238ca282cc2a3d84cc535d24190e0bef49cdcd`。领口一侧滑到肩下。
- 大图 3 `03.png`：832 × 1216，1,718,851 字节；SHA-256 `29e16314e84776a9eb9414717528f59d817a1dd092f09ed15804cbee33ee8120`。两侧肩带褪到上臂。
- 小缩略图 `01-thumb.webp`：175 × 256，9,550 字节；SHA-256 `e4293562d5e6cf86c4438b349acd8b33eeef337dda2dba6ffc5b2b4bc662675e`。
- 小缩略图 `02-thumb.webp`：175 × 256，9,324 字节；SHA-256 `0b153133964f0719f22140e2e2b80ede1de71ff6891e05e1f5208c179089bb3c`。
- 小缩略图 `03-thumb.webp`：175 × 256，10,208 字节；SHA-256 `d392ba3b887e4b6eab9cc9cad21aab829c629a906d791e2f325a1c122e393374`。

来源与处理：

- 大图来自用户 2026-09-25 提供的原样转存副本，本项目现有提示词出图；保留原 PNG 字节与内嵌数据，未裁切、未重绘。
- 小缩略图由本次上传任务从对应大图生成：RGBA → 缩放到 175 × 256（LANCZOS）→ WebP（quality 80, method 6）。不改变大图字节。
- 三张内嵌正面提示词与画面描述逐字相同（NovelAI Diffusion V5），差异只体现在成图上；图内没有 seed 与负面词字段，标为未知。
- 三张均为 RGBA PNG，体积在 jsDelivr 单文件 20 MiB 限制以内。
- 远程路径 `images/backstreet-examples/houjie/`；主项目侧的图序、剧情与地址登记见 `跑图/朝日工作区/提示词/后街-差分图序.md`。

配置用法：后街作者侧把大图与缩略图直链填进 `content.json`，用固定完整提交号的 jsDelivr 地址；本次六张全部落在提交 `44307c253debdd8853146d47601c10e397fc69d1` 里。逐段差分在 `pages[]` 的 `image` 字段填写，留空沿用上一张。
